#include "Brain.hpp"
#include <random>

double* create_weight_matrix(int size)
{
    double* a = new double[size];
    return a;
}

void populate_weight_matrix(double *matrix, int size){
	for(int i = 0; i<size; i++)	 {
		matrix[i] = 
	}
}
